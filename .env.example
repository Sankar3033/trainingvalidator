# Cloud frontend build config.
# Local split-dev: leave empty — the vite proxy forwards /api to :8000.
# Production values live in .env.production (committed), currently
# VITE_API_BASE_URL=https://trainingvalidatorbackend.vercel.app
VITE_API_BASE_URL=

VITE_ORG_NAME=Schneider Electric
VITE_APP_SHORT_NAME=Training Validator
VITE_QR_PATH_PREFIX=/getInfo
