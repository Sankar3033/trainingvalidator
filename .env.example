# Cloud frontend build config (Vercel).
# Point this at your deployed backend Vercel project URL.
# Leave empty for local split-dev (the vite proxy forwards /api to :8000).
VITE_API_BASE_URL=

VITE_ORG_NAME=Schneider Electric
VITE_APP_SHORT_NAME=Training Validator
VITE_QR_PATH_PREFIX=/getInfo
